# 講義音声まとめWebアプリ MVP仕様

## 1．目的

講義や自習の音声ファイルから，Gemini APIを使って次の3つを作るスマートフォン対応Webアプリとする．

- 文字起こし（全文）
- 要約（3行程度の一つの文章）
- 要点（3〜5個の箇条書き）

## 2．画面の流れ

1. 初回起動時にGemini APIキーを入力する．
2. 講義名を入力する．
3. 音声ファイルを1つ選択する．
4. 「実行」ボタンを押す．
5. 処理中と表示する．
6. 文字起こし，要約，要点をまとめて表示する．
7. 必要な結果をコピーする．

## 3．画面構成

画面は1ページで作る．スマートフォンでは上から下へ1列に並べる．

### 入力エリア

- 講義名入力欄
- 音声ファイル選択
- 選択したファイル名の表示
- 実行ボタン
- APIキー設定ボタン

### 結果エリア

- 文字起こしカード
- 要約カード
- 要点カード

各カードにコピーボタンを置く．

## 4．入力

### 講義名

- 必須とする．
- 1〜100文字とする．

### 音声ファイル

- 1回に1ファイルだけ選択できる．
- 対応形式はWAV，MP3，AIFF，AAC，OGG，FLACとする．
- ファイルサイズは14 MB以下とする．
- MVPではブラウザー録音機能を作らず，ファイル選択のみとする．

## 5．出力

### 文字起こし（全文）

- Geminiが生成した文字起こしをそのまま表示する．
- 編集機能は付けない．
- コピーボタンを付ける．

### 要約（3行程度の一つの文章）

- 文字起こしの内容を，画面上で3行程度になる分量に要約する．
- 箇条書き，番号，見出し，手動改行は使わず，一つの段落として表示する．
- コピーボタンを付ける．

### 要点（3〜5個）

- 文字起こしの主なポイントを3〜5個の箇条書きで表示する．
- コピーボタンを付ける．

## 6．API処理

MVPでは無料枠のレート制限にかかりにくくするため，Gemini APIへのリクエストを1回にまとめる．

1. ブラウザー内で音声ファイルをBase64へ変換する．
2. 音声と講義名を1回のInteractions APIリクエストでGeminiに送る．
3. 文字起こし，要約，要点を構造化JSONとしてまとめて取得する．
4. 取得した内容を画面の各カードに表示する．

Files APIは使用しない．生成リクエストは `store: false` とし，ファイル削除リクエストも発生させない．

画面内で保持するデータは次の形とする．

```json
{
  "transcript": "文字起こし全文",
  "summary": "3行程度の分量でまとめた一つの文章",
  "keyPoints": ["要点1", "要点2", "要点3"]
}
```

## 7．APIキー

- APIキーはソースコードに直書きしない．
- APIキーをGitHubなどの公開リポジトリに含めない．
- 初回起動時に利用者がAPIキーを入力する．
- 入力したキーはその端末の `localStorage` に保存する．
- APIキーを削除できるボタンを用意する．
- この方式は個人利用のMVP用とする．一般公開時はバックエンドでAPIキーを管理する．

## 8．エラー表示

| 状況 | 表示メッセージ |
|---|---|
| APIキーがない | APIキーを設定してください． |
| 講義名がない | 講義名を入力してください． |
| 音声ファイルがない | 音声ファイルを選択してください． |
| 対応外の形式 | 対応している音声ファイルを選択してください． |
| ファイルが14 MBを超える | ファイルサイズが14 MBを超えています． |
| APIが失敗した | 処理に失敗しました．APIキーや通信状況を確認して再度実行してください． |

処理中は実行ボタンを無効にし，二重送信を防ぐ．

## 9．MVPで作らないもの

- ログイン機能
- 履歴保存
- 文字起こしの編集
- ブラウザー上での録音
- 話者分離
- タイムスタンプ
- ファイルの分割処理

APIの詳細は [API_NOTES.md](./API_NOTES.md) を参照する．
